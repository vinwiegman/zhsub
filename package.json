{
  "name": "zhsub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mandarin + pinyin + English subtitles for YouTube and Bilibili, on every video.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "dependencies": {
    "pinyin-pro": "^3.26.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
